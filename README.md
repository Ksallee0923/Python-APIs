
### 2. **In Your Terminal** (For actual testing during development)
Run these commands directly in your terminal/PowerShell to test the API while it's running (`flask run`):

```bash
# In one terminal window:
flask run

# In another terminal window:
curl http://localhost:5000/books  # Test GET all books
